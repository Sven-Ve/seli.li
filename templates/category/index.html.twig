{% extends 'base.html.twig' %}

{% block title %}{% trans %}Categories{% endtrans %}{% endblock %}

{% block body %}
  <div class='form-lg'>
    <h1><i class="fas fa-layer-group"></i> {% trans %}Categories{% endtrans %}</h1>

    {{ include('category/_list.html.twig') }}

    <a href="{{ path('category_new') }}" class="btn btn-sm btn-success">{% trans %}New{% endtrans %}</a>
  </div>
{% endblock %}
